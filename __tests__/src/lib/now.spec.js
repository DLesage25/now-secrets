const mockedSpawn = require('mock-spawn')();

describe('Now.js', () => {
    let now = require('../../../src/lib/now');

    describe('removeNowToken', () => {
        it('', () => {
            mockedSpawn.sequence.add(mockedSpawn.simple(0));
        });
    });
});
